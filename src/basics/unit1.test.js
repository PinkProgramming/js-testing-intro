/* 
    Step 1. Structure of tests and syntax for test in JS
  */

// Create the file for the tests somewhere inside /src directory
//    JS: Make sure to include ".test.js" in the end of the filename
//    Java: Make sure to include "Test" in the end of the filename

// Declare
test("My test", function() {
  // this is where we write the test
});

// step 2. variable level
// expect(varA).toBe(varB);

// step 3. introduce array (for strings)

// RUN TESTS
